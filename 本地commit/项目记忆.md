# Fntv-Plus 影视网页端增强 · 项目记忆

## 项目定位
把 Fntv-Plus（Electron 客户端）的影视网页端增强，做成飞牛 **FPK 应用**，注入到网页版飞牛影视（`/v`），
供"不想装客户端、只在浏览器看片"的用户使用。入口 URL 变为 `/app/fntvplus/v/`（官方桌面图标包装，对用户无感）。

## 关键架构决策
- **复用，不重写**：将 `Fntv-Plus/src/preload` 逐字复制到本项目的 `src/preload/`。
  `embyWall.ts` 在模块加载时通过 `registerHook(HookType.OnReady, handle)` 自注册；
  `src/web-entry.ts` 在 DOM ready 后 `runHooks(OnReady)` 即可启动海报墙/轮播/沉浸式美化。
- **浏览器垫片** `src/shim/electron.js` 提供 `ipcRenderer`/`shell`（`settings:*` → localStorage，其余 no-op），
  由 esbuild `alias: { electron: shim }` 注入；`process.env.NODE_ENV` 用 `define` 处理。
- **打包**：`scripts/build-userjs.mjs`（esbuild）→ `dist/fntv-plus.user.js`（IIFE，约 777KB），即 FPK 要注入的 payload。
- **注入宿主（已做 · lc-002）**：FPK **Go 反代** 已实现并通过端到端验证。代码在 `src/go/`：
  - `cmd/fntvplus/main.go` 入口（flag: --port/--etc/--var/--dest/--upstream；上游优先级 upstream > FNTV_UPSTREAM > TRIM_SYS_WEB_PORT > 127.0.0.1:5666）。
  - `internal/proxy` 反代核心：剥离 `/app/fntvplus` 前缀回环影视 `/v`；text/html 注入 payload 引用块（带 `FNTV_PLUS_INJECT_BEGIN/END` 幂等标记），非 HTML（js/css/图片/视频 206）原样流式透传；`DisableCompression` 规避 gzip 注入错位；`DisableKeepAlives` 避免回环连接复用问题。
  - `internal/inject` 用 `//go:embed` 把 `dist/fntv-plus.user.js` 打进单二进制（零依赖），payload URL 带内容哈希用于缓存失效，独立端点 `/app/fntvplus/__payload__/` 长缓存服务。
  - `internal/admin` 管理页 + `/api/settings`（增强总开关，存 `TRIM_PKGETC/config.json`）。
  - `cmd/mockupstream` 本地联调假影视（含可 Range 视频）。
  - `integration_test.go`：httptest 起 mock 上游，断言注入/206 透传/设置读写/幂等。已 `go test` 全绿；双架构交叉编译（linux amd64/arm64）通过。
  - 零系统文件改动；影视升级不失效、不与 fndesk 冲突、卸载零残留。
- **后端语言 Go**（单二进制，零依赖），platform 需分 `x86`/`arm` 两包（`scripts/build-fpk.sh` 一键交叉编译到 `server/`）。

## 本地 commit 约定（本项目）
- 每次改动自动本地 commit + 打轻量 tag `lc-NNN`（从 `lc-001` 起，与 Fntv-Plus 仓库序列独立）。
- 只暂存文本源码（`src/`、`docs/`、`scripts/`、`demo/`、`配置`、`本地commit/`）；`node_modules/`、`dist/`、`*.log` 不入库（见 `.gitignore`）。
- **不 push**、**不带 WorkBuddy 署名**（全局 `commit-msg` hook 自动剥离 Co-Authored-By）。
- 回退：`git -C D:/GitHub/Fntv-Plus-Desktop/fntvplus checkout lc-NNN`；查 hash：`git rev-parse --short lc-NNN`；回最新：`git checkout main`。

## 外部播放器（决策）
- 只做 `fntv://` **单向唤起**本机播放器（能调起即可），进度回传网页端先搁置（详见开发文档第七.5 节）。
- 参考实现 `D:\GitHub\Fntv-Plus-Desktop\lc-1105-fntv-protocol.patch`（从 Fntv-Plus 导出，暂未合入）。

## 参考路径
- Fntv-Plus 现有代码：`D:\GitHub\Fntv-Plus\src\preload`
- 开发文档：`docs/完整实施方案.md`、`docs/功能可行性评估.md`
- 冒烟验证：Playwright 加载 `demo/host.html`，断言 `window.__FNTV_BOOT__` 就绪 + 样式注入 + 零报错。


## 版本约定（2026-09-08 用户明确）
- 每次更新，打包版本号 +0.1（如 0.3.0 → 0.4.0）。manifest 的 version 与 src/go/cmd/fntvplus/main.go 的 appVersion 常量两处必须同步改。
