# 本地 Commit 日志（fntvplus 项目 · 回退锚点）

> 本文件为 fntvplus 项目的本地 commit 回退锚点日志。
>
> - **标识符** = 本地轻量 tag `lc-NNN`（如 `lc-001`）。回退：`git -C D:/GitHub/Fntv-Plus-Desktop/fntvplus checkout lc-NNN`
> - 查 hash：`git rev-parse --short lc-NNN`。回到最新：`git checkout main`
> - 本地 commit/tag **不 push**（遵循"不主动 push"约定）。
> - 规则：每次改动由 AI 自动本地 commit，并 append 一行到此表，同时打 `lc-NNN` tag。
> - 本项目从 `lc-001` 开始编号（与 Fntv-Plus 仓库的 lc 序列互不干扰）。

| 标识符 | 日期 | 说明 |
|--------|------|------|
| lc-001 | 2026-09-07 | 初始化项目：复用 Fntv-Plus preload（embyWall 海报墙/轮播/沉浸式美化 + core/*）逐字复制到 src/preload/；新增 web-entry.ts（DOM ready 后 runHooks(OnReady) 触发自注册的 embyWall）+ src/shim/electron.js 浏览器垫片（settings:* → localStorage，其余 no-op）+ scripts/build-userjs.mjs（esbuild 打包为 dist/fntv-plus.user.js）；dev 静态服务 scripts/serve.mjs + 冒烟 scripts/smoke.mjs + demo/host.html；建立本地 commit 工作流（lc-001 起）与 .gitignore；完善开发文档 §5.5 复用机制 |
| lc-002 | 2026-09-07 | 实现方案 D「Go 反代注入后端」（M1 骨架跑通）：src/go/ 下 cmd/fntvplus（flag + 上游推导）+ internal/{config,inject,proxy,admin}；反代剥离 /app/fntvplus 前缀回环影视 /v，text/html 注入 payload 引用块（带 FNTV_PLUS_INJECT_BEGIN/END 幂等标记），非 HTML（含视频 206）原样流式透传；payload 经 //go:embed 打进单二进制、独立端点长缓存 + 内容哈希版本；管理页 + /api/settings 增强总开关（存 TRIM_PKGETC/config.json）；cmd/mockupstream 联调假影视；integration_test.go 断言注入/206/设置/幂等，go test 全绿，linux amd64/arm64 交叉编译通过。配套 FPK 打包骨架：manifest、config/{privilege,resource}、cmd/{main,install_callback,uninstall_callback}、app/ui/config 桌面入口、wizard/install、scripts/build-fpk.sh；清理构建产物并把 server/ 等加入 .gitignore；更新开发文档 §5.5 与项目记忆（注入宿主：未做→已做） |
| lc-003 | 2026-09-07 | 一键打包成飞牛应用（exe）：新增 tools/buildfpk/main.go（Go 源码）编译为根目录 build-fpk.exe——自动查 tools/fnpack.exe 或 Downloads 下 fnpack*；一键链路 = node 重建 dist payload → 同步到 //go:embed 目录 → 交叉编译 linux/amd64 后端到 app/server/fntvplus（fnpack 只打包 app/ 内容，根目录 server/ 不进包）→ fnpack build 产出 fntvplus.fpk；附 一键打包.bat（chcp 65001 + pause）。实跑验证：fpk 5.2MB，app.tgz 含 server/fntvplus + ui/config，与 cmd/main 的 $TRIM_APPDEST/server/fntvplus 对齐。补齐 fnpack 必需文件：ICON.PNG/ICON_256.PNG（占位图）+ cmd/{install_init,uninstall_init,config_init,config_callback,upgrade_init,upgrade_callback}；发现 wizard/install 目录存在即触发严格 schema 校验（空目录也失败）→ 暂移除向导，样例存 docs/wizard-install.example.json 待研究官方 schema；修 install_callback payload 路径（app/server/payload → server/payload，app/ 解包落 TRIM_APPDEST 根）；build-fpk.sh 输出改到 app/server/（arm64 加 WITH_ARM64=1）；.gitignore 增加 build-fpk.exe/tools//*.fpk |
| lc-004 | 2026-09-08 | 修复 NAS 安装后 not found（后端未启动）：根因是 Windows fnpack 打包所有文件权限位 0666、二进制无可执行位 → cmd/main 的 nohup Permission denied。修复：install_callback 与 cmd/main 启动前 chmod +x 二进制；cmd/main 加诊断日志（start/stop 各步落 $TRIM_PKGVAR/fntvplus.log，BIN 缺失时 ls TRIM_APPDEST 辅助定位解包路径）。管理页升级（用户要求"配置 UI + 实时日志"）：新增 /app/fntvplus/api/status（版本/上游连通性探测/payload 哈希/日志路径）+ /api/logs（tail N 行）；管理页加「运行状态」卡（5s 自动刷新，上游不通给 FNTV_UPSTREAM 提示）与「实时日志」卡（行数可选 + 自动刷新 + 贴底滚动）。go vet/test 全绿，mock 上游端到端验证 status/logs/注入。版本 0.2.0（manifest 与 Go const 同步） |
| lc-005 | 2026-09-08 | 修复桌面入口 not found（v0.3.0）：查官方文档 developer.fnnas.com 纠正端口服务模式——不存在 /app/<appname> 自动网关路由，app/ui/config 必须带 protocol=http + port=22350（fnOS 直连 NAS:端口打开入口）；后端监听 127.0.0.1 → 0.0.0.0；未知路径兜底由 404 改为全量反代上游（SPA 静态资源不全在 /v/ 下）；官方文档还确认 wizard/install 是合法目录（install/upgrade/uninstall/config 四类）、manifest 全字段语义。E2E 验证注入/admin/status/未知路径透传后重打包 |
| lc-006 | 2026-09-08 | 上游地址管理页热修改：config.upstream 运行时生效（代理每请求取生效值，空/非法回退启动推导值）；状态卡上游地址可编辑+保存；status 上报 upstream/upstream_default；启动仅在配置为空时写入推导值不覆盖自定义。E2E 双向验证（清空→回退不通✓，热切→连通+注入✓） |
| lc-007 | 2026-09-08 | 上游不可用出错页升级为自助修复页：502 内嵌上游地址输入框（预填当前生效值）+「保存并重试」自动刷新，不再是一行纯文本；用户反馈找不到填地址入口。干净环境回归：502✓/预填✓/保存后注入✓ |
| lc-008 | 2026-09-08 | 修复增强不生效：embyWall 的 isFntvTvPage()（preload/core/pageMode.ts）要求 pathname 以 /v 开头，桌面入口 /app/fntvplus/v/ 不满足 → TV 改造全跳过，用户看到原样影视页；桌面入口 URL 改为 /v/（后端裸 /v/ 路由已有），SPA 视角与桌面版 Electron 一致；代理加注入成功日志便于实时日志确认 |
| lc-009 | 2026-09-08 | 应用中心打开应用 → 配置页（desktop_applaunchname 改指 fntvplus.Settings），配置+状态+实时日志作为主入口；影视 Plus 仍走桌面图标 |
| lc-010 | 2026-09-08 | 桌面入口 iframe→url：点应用在新浏览器标签页打开完整网页（非 fnOS 应用小窗），两入口（影视 Plus/设置）都改 |
| lc-011 | 2026-09-08 | 日志入口三件套：payload 诊断回传（web/diag.ts 拦 [EmbyWall]/[fntv] console + 全局错误 → POST /api/client-log）；后端落 client.log（5MB 轮转）+ logs 接口合并后端/前端两路；注入页左下角半透明「日志」按钮 → 设置页。轮播墙失败原因从此可在实时日志看到 |
| lc-012 | 2026-09-08 | 修海报墙全挂：根因=网页端无 fnos-gen-authx 签名器（undefined 头被序列化发出去全拒）。diag 钩 XHR/fetch 捕获页面自身 Authx 供 shim 回放；fetch 剥离坏 Authx 头（sys/img cookie 直取）；shim 默认开 embyWall 日志总开关（[DIAG] fetchImg 带状态码可见）。用户日志证据：item/list invalid sign code=5000 → DOM 兜底选出 18 项 → lc-768 全部海报无法加载 |
| lc-013 | 2026-09-08 | 版本 0.4.0。新约定：每次更新打包版本号 +0.1（manifest 与 src/go/cmd/fntvplus/main.go 的 appVersion 两处同步改） |
| lc-014 | 2026-09-08 | v0.5.0 彻底修签名：从桌面版 fnosAuth.js 提取 Authx 算法（KEY+SECRET+md5 拼接），shim 本地 genAuthx 真签名（md5.ts 移植 uuid 包实现，5/5 向量过）；不再依赖捕获回放，item/{guid} 等任意接口可签 |
| lc-015 | 2026-09-08 | v0.6.0 飞牛影视特化：设置面板精简（通用整页删/外观只留两滑块/播放只留片头片尾+滚轮横滚；弹幕/账号/网络/手柄/诊断/关于保留；隐藏原始播放按钮游离）。手术式改法：cats 数组裁剪 + buildAppearanceControls 瘦身，孤儿卡不挂载即不可见，回填处 null/变量守卫已核对 |
| lc-016 | 2026-09-08 | v0.7.0 外观卡恢复主题模式三选一（浅色/深色/跟随系统）——用户要求保留 |
| lc-017 | 2026-09-08 | v0.8.0：网络分类删「网络与代理」卡（自定义代理+TMDB直连保留）；侧边栏左下角「切换系统页面/切换NAS界面」按钮移除（侧栏只剩 设置→软件反馈建议） |
| lc-018 | 2026-09-08 | v0.9.0 修反馈弹窗：qrcode.png 内嵌 Go 后端直出（/app/fntvplus/qrcode.png），shim app:qr-image fetch→dataUri；app:open-external → window.open 新标签。E2E：端点 200/image/png/97619B/有效 PNG 头 |
| lc-019 | 2026-09-08 | v0.10.0 日志入口收敛：左下角悬浮按钮删；诊断与日志卡四按钮（日志文件/报错日志/导出日志/MPV日志）删，替换为实时日志查看器（/api/logs 轮询 5s 自动刷新，合并后端+前端两路，防定时器泄漏） |
| lc-020 | 2026-09-08 | v0.10.1 删两条刷屏日志：fetchImg 成功逐条打→仅失败留证据；lc-925 图标反色每次调度打→删除（异常日志保留） |
| lc-021 | 2026-09-08 | v0.11.0 补回观影记录：watchHistory.ts 插件从未被 web-entry import（lc-001 只挂 embyWall）——补 import；Authx 真签名使网页端能拉已观看数据 |
| lc-022 | 2026-09-08 | v0.12.0 外观卡只留主题模式三选一：亚克力透明度/背景模糊两滑块移除（buildAppearanceControls 删除；启动默认值不变） |
| lc-023 | 2026-09-08 | v0.13.0 删「切换NAS界面」残余：非影视页零注入（返回影视/外部播放浮动按钮删除，IPC no-op 残留根源）；视频预览外放保留（lc-389） |
| lc-024 | 2026-09-08 | v0.14.0 实时日志「自动刷新」开关改胶囊样式（原生 checkbox 深色主题显示异常→track+knob 自绘，与面板开关统一） |
| lc-025 | 2026-09-08 | v0.15.0 设置面板重排：8类→6类（外观=主题+界面交互；播放=片头片尾+手柄；弹幕；账号与网络合并；诊断与日志；关于），界面与浏览卡更名界面交互 |
| lc-026 | 2026-09-08 | v0.16.0 账号与网络全面移植：internal/bridge 包（fnOS 签名桥/白名单外代理/TMDB 图+logo+详情/Trakt 凭证+设备授权+scrobble+sync/Bangumi 日历/豆瓣状态占位）；config Extra 平铺任意键（自定义 Marshal/Unmarshal）；settings API GetMap；shim 全通道映射（settings 服务端持久化+trakt 9 通道+tmdb 3 通道+bangumi/douban）。待续：豆瓣评分增强/Bangumi 同步核心/Trakt 搜索匹配细化 |
| lc-027 | 2026-09-08 | v0.17.0 移植收尾：douban/watched（全库钻取进度分析，桌面同形状）、douban/enrich（TMDB 分类/评分+豆瓣评分尽力抓）、bangumi/sync-progress（搜条目→标集/条目）；修 douban/status 路由误删；shim 对应三通道映射 |
| lc-028 | 2026-09-08 | v0.18.0 免梯子直连移植：tmdb/update-ip（CheckTMDB hosts→api/image IP 持久化，force 语义与桌面一致）；tmdbClient IP 直连（DialTLS 连 IP+SNI 用域名）；配自定义代理可走代理拉取。被墙时错误文案与桌面一致 |
| lc-029 | 2026-09-08 | v0.19.0 日志治理：后端启动清零 client.log（日志=本次运行会话）；diag 时间戳带日期。用户看到的 [lc-925] 刷屏是旧会话历史条目（payload 已无此日志，repo/dist/embed 三处 0 命中已验证） |
| lc-030 | 2026-09-08 | v0.20.0 实时日志「自动刷新」开关移除——始终自动刷新（5s），保留手动刷新按钮 |
| lc-032 | 2026-09-08 | v0.21.0 品牌化：display_name=Fntv-Plus；双入口独立图标（fntv_*/setting_* 64+256，2048 原图归档 docs/）；应用中心图标换新 |
| lc-033 | 2026-09-08 | v0.22.0 图标统一：全部入口/应用中心统一蓝牛主图标；冰块设置图白边为其画风自带（像素分析：全图不透明），按统一要求弃用 |
| lc-034 | 2026-09-08 | v0.23.0 图标统一为冰块图（应用中心+双入口全部用户指定冰块图） |
| lc-035 | 2026-09-08 | v0.24.0 图标更新：用户重制 jpeg 两张——主入口/应用中心用主图，设置入口独立图标（64/256 双档），原图归档 docs/ |
| lc-036 | 2026-09-08 | v0.25.0 修 TMDB search 401：TMDB 双格式鉴权（v3 api_key / v4 Bearer JWT 双支持，与桌面一致），非 200 附 Key 格式诊断 |
| lc-037 | 2026-09-08 | v0.26.0 修免梯子直连保存后显示未开启：set-tmdb-direct 复合对象拆 tmdbDirectConnect/tmdbDirectIp 两字段（与桌面 config 对齐），Go 读键同步修正；连带修 dandanplay-credentials 双参数拆分 |
| lc-038 | 2026-09-08 | v0.27.0 冰块图标去白框：内切 7% 裁剪+自绘 22% 圆角透明边，四角 alpha=0 验证通过，预览 docs/ice-cube-cropped-preview.png |
| lc-039 | 2026-09-08 | v0.28.0 修 TMDB 卡数据不全：忠实移植 fetchShowDetails（append_to_response 聚合+include_image_language）+ normalizeShow 完整形状（评分/类型/演职员/主创/外链/预告/平台/推荐/上下集）+ 长标题递进搜索 + tmdb:season-episodes 双语分集 |
| lc-040 | 2026-09-08 | v0.29.0 修桌面图标缓存残留：图标文件版本化改名 fntv_v2_64/256.png（fnOS 桌面按路径缓存，同名不失效），双入口统一引用；设置入口也换冰块图 |
| lc-041 | 2026-09-08 | v0.30.0 全插件挂载+通道全覆盖：25 插件全挂（对齐桌面）；fs/path 垫片；shim 113 通道 0 缺口（审计脚本验证） |
| lc-042 | 2026-09-08 | v0.31.0 网页端用不到的全删：titlebar/dialogUI 插件不挂载；embyWall 删孤儿卡构建代码约 1150 行（更新/补丁/解锁/历史版本/播放器/退出/语言/插帧/渲染/系统桌面/轮播Logo/网络与代理）；shim 同步清理 |
| lc-043 | 2026-09-08 | v0.32.0 shim 清理已死通道 no-op 分支（window-*/fnos-dialog 等无调用方） |
| lc-044 | 2026-09-08 | v0.33.0 B站扫码登录+两测试移植：bili/qr-generate+qr-poll（cookie 持久化，状态码映射同桌面）+status/manual/clear+qr-lib 内嵌直出；danmu/test（试搜校验）；proxy/test（http 代理拉 bgm.tv，socks5 不支持提示）。E2E 真实拿到 B站 QR key/url |
| lc-045 | 2026-09-08 | v0.34.0 修全插件挂载页面变原生：banner 挂 window.require/__dirname 兜底 + ipcRenderer.off 补全 + diag 最先自动装钩子（模块错误可定位）。本地冒烟全绿 |
| lc-046 | 2026-09-08 | v0.35.0 修网页侧边栏设置打不开：根因①lc-042 删「退出行为卡」遗留 exitEls 孤儿引用 → buildSettingsPanel 构建期 ReferenceError → 面板从未挂载 → 侧栏「设置」点击静默无反应（错误还被 boot try/catch 吞成 console.error，pageerror 抓不到）；根因②demo 宿主 console.log 直写 #log → DOM 变更 → carousel observer → injectCarousel → 打日志 → 微任务饿死主线程。修：删 refreshExit/hover/_exitMode 孤儿代码；buildSettingsPanel 加 try/catch+console.error（diag 回传）；openSettingsPanel 面板缺失自愈重建；carousel observer 加 1s>40 次风暴熔断；demo 宿主日志改 rAF 批量 flush + 补仿侧栏抽屉骨架；新增 stack-probe/mo-dump 诊断脚本；test-panel 补 PANEL_DISPLAY/BOX/MASK/WARN 断言。验证：BTN=true/挂载=true/display=flex/680x620/6 分类/无错误；心跳 10/10 活 |
| lc-047 | 2026-09-08 | v0.36.0 修二级详情页「剧集信息：标题为空」：渲染层 extractTmdbId() 返回字符串型 id，桌面版用正则接受数字字符串、Go handler 用 int64 接收 → 反序列化报类型错又被 `_ =` 丢弃 → tmdbId 静默变 0 → 退化为按标题搜索 → 季页季对象无标题 → 报错。修：新增 flexInt64（兼容数字/数字串/带空格/null/浮点，宽容归零不返 error），两个 handler 的 tmdbId/seasonNumber 改用它；解析错误不再吞，落 fntvplus.log；新增 [fntv-bridge] 请求日志；错误文案带诊断信息；新增 tmdbshow_test.go 三条回归（字符串 id 必被吃下 / 有 id 无标题也直取 / 同结构体字段不被牵连）。go build + go test 全绿 |
| lc-048 | 2026-09-08 | v0.37.0 外观卡补回两项被误删的开关：v0.12.0 精简时把 buildAppearanceControls() 整函数删除（当时只想移亚克力/模糊两滑块），连带删了同函数体内的「首页轮播图样式」四选一（lc-780）与「剧集详情页美化」开关（lc-980），造成功能在、入口没了。改：按桌面版原样补回两项到 secBodyAppearance——轮播样式点选写 localStorage 并回 /v 重载；美化开关 checked=!detailBoxless、写 settings:set-detail-boxless 并立即 apply/teardown，暴露 _beautifyToggle/_beautifyPaint（回填链路本就完好）。test-panel 补 BEAUTIFY_TOGGLE/CAROUSEL_STYLE_SEG/CLICK 三断言。验证：开关 present+checked=true、4 按钮、点击 4→2、无错误 |
| lc-049 | 2026-09-08 | v0.38.0 「Fntv-Plus 设置」桌面入口换新图标（用户提供根目录 Fntv-Plus-Setting.jpeg，玻璃齿轮图）：scripts/make-setting-icon.py 内切 10% 裁边 + 22% 圆角透明边（与 lc-038 做法一致），输出 64/256 两档；版本化命名 setting_v2_{64,256}.png（fnOS 桌面按路径缓存图标，同名不失效，lc-040 教训）；app/ui/config 中 fntvplus.Settings 的 icon 改指 images/setting_v2_{0}.png（主入口 fntv_v2 不动）。解包验证 app.tgz 含 setting_v2_64/256.png + 新 config。四角 alpha=0 验证通过，预览 docs/setting-icon-v2-preview.png |
| lc-050 | 2026-09-08 | v0.39.0 代理设置优化（侧边栏设置→账号与网络→自定义代理卡）：①删用不了的 SOCKS5 协议——Go 端 customProxy 仅 proxyTest 与 tmdbUpdateIP 两处消费、均用 http.ProxyURL 不支持 socks5，UI 下拉框/回填分支/描述文案一并去掉；②优化配置方式——前端新增 validateCpAddr 本地预校验（host:port + 端口 1-65535），保存/测试前即时反馈不必等后端；③优化测试方式——proxyTest 从单测 bgm.tv 改为依次试 TMDB（主，常需翻墙）+ Bangumi（次）两真实目标，任一连通即 ok 并返回命中目标与延迟(info)，超时 12s→10s、错误文案去 socks5；新增 bili_test.go 边界回归（空/非法/socks5 均被拒）。go build/test 全绿，fpk 重打 v0.39.0 |
| lc-051 | 2026-09-08 | v0.40.0 修首页每日放送三源全无数据：①TMDB/豆瓣源整条链路从未移植——shim 无 tmdb:discover/douban:discover 映射（兜底返回 undefined）且 Go 无 discover 路由；②Bangumi 源 Go 端只透传 bgm.tv 原始数组，前端期望桌面版 {ok,items} 形状，res.ok 恒 undefined → 必然「获取失败」，shim 还用 catch(()=>[]) 吞错。修：新建 bridge/hot.go 忠实移植桌面版三 handler——tmdbDiscover（复用 b.tmdbGet 鉴权+免梯子直连，/discover/movie+/discover/tv 并行合并 normalize 排序）、doubanDiscover（Rexxar movie_hot_gaia+tv_hot，移动 UA+Referer）、doubanImage（海报防盗链代理，桌面 UA+Referer 转 dataUrl）；bangumiCalendar 重写为桌面版 fetchCalendar 同款（展平/过滤 type 2/6/同 id 去重/收藏+评分排序→{ok,items}）；shim 补三条映射 + bangumi 失败改回 {ok:false,error} 不吞错。顺手修 config.go MarshalJSON 值接收者拷贝锁（go vet 报错，改指针接收者，序列化点核对安全：save 传指针/GetMap 手动平铺）。新增 3 条路由 tmdb/discover、douban/discover、douban/image。go build/vet/test 全绿，fpk 重打 v0.40.0 |
| lc-052 | 2026-09-08 | v0.41.0 修每日放送 Bangumi 超时（用户已配代理且 proxyTest 通过仍超时）：根因=lc-051 移植 fetchCalendar 时漏了桌面版 withTransport（代理 > 免梯子直连 > 系统 DNS，代理与直连互斥）——Go 端只用 b.client 直连，customProxy 根本没被用上。修：①hot.go 新增 fetchWithTransport（配了自定义代理先走代理 12s，失败直连 8s 兜底重试，非 200 也换路），bangumiCalendar 改用它，未配代理时错误文案带「可配自定义代理」指引；②bridge.go tmdbClient 补同一语义的代理优先分支（TMDB discover/img 同样此前用不上代理），20s 超时。go build/vet/test 全绿，fpk 重打 v0.41.0 |
| lc-053 | 2026-09-08 | v0.42.0 修自定义代理「测试通过但实际不走代理」：用户 proxyTest 415ms 通过但 Bangumi 仍直连超时——报错只有「直连」一路暴露后端没拿到代理。根因：shim 无 settings:set-custom-proxy 特例，通用逻辑把 args[0]（enabled 布尔→"1"）误存进 customProxy 键，URL(args[1]) 被丢弃 → 后端 customProxy 恒非法；proxyTest 成功是因测试按钮直传表单 URL 不依赖存储。修：①shim 补 set-custom-proxy 特例（照 set-tmdb-direct 模式，存桌面 config.json 同款两字段 customProxyEnabled+customProxy）+ get-custom-proxy 特例（回填需要 {enabled,proxyUrl} 复合形状，对齐桌面 getCustomProxyConfig）；②Go 新增统一 customProxyURL()（桌面 pickProxyUrl 语义：HTTPS_PROXY 环境变量优先 > customProxyEnabled=true 且 customProxy 合法 http(s)，脏数据如历史误存 "1" 一律拒绝），替换 fetchWithTransport/tmdbClient/tmdbUpdateIP 三处消费点。注意：修复后需重新点「保存」覆盖脏数据。go build/vet/test 全绿，fpk 重打 v0.42.0 |
| lc-054 | 2026-09-08 | v0.43.0 删除网页端手柄控制功能：播放控制（暂停/快进/倍速/切集）依赖 IPC media:control（桌面主进程 controlCurrentPlayer 控 mpv），网页端 shim 是 no-op——核心功能完全无效，功能半残。删：①web-entry.ts 不再挂载 gamepad/gamepadFocus（esbuild 摇树，payload 验证 getGamepads=0、手柄设置卡=0、无 secGamepad/gpApi 残留引用）；②embyWall.ts 删「手柄设置」卡构建段 261 行（L2499-2759），播放分类只留跳过片头片尾。i18n 手柄词条无 UI 引用保留不动（与桌面结构对齐减 diff）。fpk 重打 v0.43.0 |
| lc-055 | 2026-09-08 | v0.44.0 自定义代理「保存不住」排查+反馈加固：全链路审查（前端保存→shim 特例→后端 Update merge→GetMap→回填）代码无 bug、TestSettingsAPI 全绿、无鉴权拦截。结论：用户大概率是①还在 ≤v0.41.0 旧包（保存 bug 本是 lc-053/v0.42.0 才修），或②升级后有旧版脏数据（customProxy 存的是布尔 true，回填视为空＝看起来没保存），重新保存一次即可覆盖。加固：①保存/重置按钮显式校验响应（apiPost 对错误码不 reject 会假成功——resp.customProxy 非字符串即报「后端未确认持久化」）；②shim get-custom-proxy 加 dirty 标记（customProxy 非字符串=旧版脏数据），回填为空且 dirty 时提示「检测到旧版残留数据，请重新填写并保存」。发现：esbuild charset=ascii 把中文转义成 \uXXXX，grep 中文验证 payload 会假阴性（lc-054 的「手柄设置=0」实际是转义假象，真验证靠 getGamepads=0）——**payload 验证必须用 ASCII 关键词**。fpk 重打 v0.44.0 |
| lc-056 | 2026-09-08 | v0.45.0 修演员详情「拉取 TMDB 完整作品失败」：personWorks 走 person:tmdb-credits/brief 两通道，shim 兜底返回 null（标注「链路待接」）——又一个从未移植的数据链路。新建 bridge/person.go 忠实移植桌面版 personTmdb.ts 管线：fnOS person/{guid}（签名请求+前端转 cookie）→ imdbId；person/item/list job×5 分页 200 → 库内 owned 匹配键（规范化中英标题+tmdb_id+imdb→tmdb find 兜底）；TMDB find → person id → combined_credits 全量 cast（去重+日期降序）→ owned 标记+库内 guid 回填；brief（职业分类中文映射/生日/出生地/代表作 top2）。缓存=进程内存 TTL 6h（桌面为磁盘 7d/30d+SWR，NAS 后端常驻等效防 TMDB 限流），失败不缓存。路由 /bridge/person/{credits,brief}，shim 两通道改 apiPost 带 document.cookie。guid 校验 32hex 同桌面。go build/vet/test 全绿，fpk 重打 v0.45.0 |
| lc-057 | 2026-09-08 | v0.46.0 修观影记录失效：主数据流 douban:get-watched-items 原走后端 /bridge/douban/watched 转发（callFnOSJSON 带 Authx+前端转发的 document.cookie）——但 fnOS 会话凭证 Trim-MC-token 是 httpOnly，document.cookie 拿不到 → 后端请求未登录 → 空列表回落示例数据。桌面版无此问题（Electron session 读 cookies 含 httpOnly）。修：shim 新增前端直连钻取 collectWatchedItemsFrontend（fnosApi=fetch credentials:'include' 自动带全部会话 cookie 含 httpOnly + shim 本地 genAuthx 签名，与海报 fetchItemPoster 同款已验证路径），忠实移植钻取逻辑（item/list 全库 → 6 并发逐项 movie 直读/剧集季→集累加 → 仅保留有观看者 → buildLocalWatchItem 同形状含 last_played/progress/total_runtime_ms）。Go doubanWatched 保留未删。shim 语法经 esbuild bundle 校验。fpk 重打 v0.46.0 |
| lc-058 | 2026-09-08 | v0.47.0 修轮播刷新后海报大面积不显示（个别显示）：根因=刷新瞬间轮播每张 slide 的主图+海报+logo 同时发起几十个并发 fetchImageAuth，无任何并发限制——浏览器同域 HTTP/1.1 仅 ~6 并发连接，排队请求叠加超时窗口、fnOS 服务端也被瞬时并发打满 → 大批超时/失败。修：images.ts 加全局并发闸门 imgGate（IMG_MAX_CONCURRENT=5，超出排队依次拉取，含 strm 重试整体入闸），桌面版同文件共用同样受益。排查中排除：熔断仅跳过本秒且有 watchdog 兜底、reveal 链路多重兜底无死锁、超时 15s 足够。若闸门后仍复现需用户端实时日志定位（[DIAG] fetchImg 失败/超时、MutationObserver 风暴、carousel lost 三类行）。fpk 重打 v0.47.0 |
| lc-059 | 2026-09-08 | v0.48.0 删首页「热门剧更新」浮层的「↻ 刷新」（强制刷新）按钮：该按钮设计用途是绕过桌面版 24h 磁盘缓存强制重拉，网页版数据通道（Go bridge）本就无磁盘缓存每次实拉，按钮无意义。删：hotUpdates.ts 里按钮 HTML、refreshBtn 声明、click listener、loadBg/loadTm 的 4 处 disabled/loading 状态行（顺带清掉两处空 finally）、深浅两套 CSS。grep 验证 payload 零残留。fpk 重打 v0.48.0 |
| lc-060 | 2026-09-08 | v0.49.0 删启动加载骨架遮罩（lc-1084 bootCover）：桌面版为遮「强刷后原生页裸奔闪现 ~1.5s」而加（隐藏 #root + 全屏 shimmer 骨架，轮播注入/离首页/12s 兜底揭示），网页端不需要——刷新期间由 fnOS 原生加载进度条承担。删 embyWall.ts 的 import + armBootCover() 调用（esbuild 摇树，payload 验证 fntv-boot-cover=0、armBootCover=0），bootCover.ts 源文件保留不打包。fpk 重打 v0.49.0 |
| lc-061 | 2026-09-08 | v0.50.0 接通网页端 Bangumi 同步（原生网页播放）：桌面版双同步挂在主进程 media.ts 的 MPV 进度事件上，网页端原生播放无 MPV → 完全没有触发点；且后端 bangumiSyncProgress 自查 play/info 依赖 document.cookie（httpOnly 断链，lc-057 同款）。修：①新建 preload/web/playSync.ts 触发器——包装页面 fetch/XHR 拦截 fnOS 原生前端的 POST /v/api/v1/play/record（浏览器自动带全会话 cookie 鉴权可靠）→ 提取 item_guid/ts/duration → 前端直连 play/info（同源+genAuthx）拿条目元数据 → invoke bangumi:sync-progress；②Go bangumiSyncProgress 改造：入参 {guid,percentage,item}（删 play/info 依赖），后端读 settings 检查开关(bangumiSyncEnabled)+阈值(bangumiSyncThreshold 默认80)；③去重 marked/missed 会话 Set + 3s 全局节流 + 类型白名单 Movie/Episode/TvSeries/TV。**豆瓣同步网页端暂无法落地**：标「在看/看过」需要豆瓣账号 cookie，网页端无登录途径（桌面版靠内嵌浏览器），需后续加手动 cookie 配置。坑：esbuild treeShaking 会把「import 了绑定但未使用」的模块整体摇掉（即使模块体有自装调用）——自装模块必须用 side-effect import（import './x'）。go build/vet/test 全绿，fpk 重打 v0.50.0 |
| lc-062 | 2026-09-08 | v0.51.0 豆瓣「手动粘贴 Cookie」接通（仿 B 站兜底方式）：设置面板豆瓣卡 UI 桌面版早已存在（粘贴框+保存按钮），但三个通道 shim 未映射——点了就「保存失败：未知」。修：①shim 补 douban:manual-cookie（存 NAS config doubanCookie 键，settings 通用通道）/douban:logout（清键）/douban:open-login（网页端无内嵌浏览器，返回引导用手动粘贴）；②shim 挂 window.__FNTV_WEB__ 环境标志，豆瓣卡网页端隐藏「扫码登录」按钮（桌面版不受影响）；③粘贴框下加获取方法提示（浏览器登录豆瓣→F12→Network 任选 movie.douban.com 请求→复制完整 Cookie，须含 dbcl2 与 ck；手机端切电脑版网页再取）；④Go doubanStatus 改按 doubanCookie 判定登录态+回传 enabled（修面板开关恒 false）；⑤Go fetchDoubanRating 带存储 Cookie+Referer 抓条目页（此前无 cookie 常被 418 拒→评分拿不到，配 Cookie 后豆瓣评分立即受益）。**豆瓣「在看/看过」标记同步本轮未做**（需移植 markInterest/getCk/douban_id 解析链，大工程，触发器 playSync 已在位可后续接）。go build/vet/test 全绿，fpk 重打 v0.51.0 |
| lc-063 | 2026-09-08 | v0.52.0 豆瓣「在看/看过」标记同步落地：新建 bridge/doubansync.go 忠实移植桌面版写入链——markInterest（POST movie.douban.com/j/subject/{id}/interest，ck CSRF + interest=do/collect，成功判定 status=success|r=0，401/please login 判 cookie 失效清 ck 缓存）+ ck 三级获取（cookie 内→会话缓存→拉条目页 Set-Cookie/HTML 正则解析）+ douban_id 解析（条目 douban_id 字段→系列缓存「剧名|类型|年代」→subject_suggest JSON 标题打分搜索+搜索页 HTML 兜底）+ markWithRetry（1 次重试，cookie 失效不重试）+ 豆瓣请求并发闸门(2)。触发：playSync 扩展——play/record → douban:sync-progress（首有效进度即标在看，无百分比门槛，后端 stateMap 去重+独立 3s 节流；is_watched=1 直接标看过且绝不降级）；拦截 /v/api/v1/item/watched（飞牛「标记为已观看」按钮）→ douban:sync-watched 标看过。开关 doubanEnabled + Cookie 均为设置面板配置。play/info 加 inflight 并发去重。go build/vet/test 全绿，fpk 重打 v0.52.0 |
| lc-064 | 2026-09-08 | v0.53.0 修 v0.52.0 整页变原生：playSync.ts 顶层调用 logger.component('play-sync')——网页版 core/logger.ts 只有 debug/info/warn/error/log（component 是桌面版专属）→ 模块加载即抛 TypeError → IIFE 中断 → 整页原生（lc-045 同款机制；v0.50.0 引入，用户从 v0.43.0 直装 v0.52.0 首次踩中）。修：改用 info/error + 消息前缀（logger 对象仅封装两方法）。教训：**网页端新增模块只能用 core/logger.ts 实际存在的方法**；顶层调用任何 API 前必须确认网页端 shim/复用模块有该成员——顶层崩=全插件灭。回归：test-panel.mjs 全绿（PANEL_MOUNTED=true/6 分类/ERRORS=none），logger.component payload 残留=0。fpk 重打 v0.53.0 |
| lc-065 | 2026-09-08 | v0.54.0 管理页日志优化（用户四点需求）：①前后端分开——日志卡加「后端日志/前端日志」tab，各自独立拉取（src=backend|web）；②一键复制——复制按钮复制当前 tab 内容，clipboard API 在 http://IP 非安全上下文不可用 → execCommand 选区兜底；③自动滚动——去掉原「贴底 30px 内才滚」的 stick 判定（易失效），改每次刷新无条件 scrollTop=scrollHeight；④只显示当前版本——admin.Info 加 StartTime（proxy 装配时 time.Now()），LogsAPI 后端日志按行首时间戳过滤（两种格式：Go log「2006/01/02 15:04:05」+ 脚本 date「2006-01-02 15:04:05」），无时间戳行跟随前一行归属，早于启动时间的历史版本段落全部剔除（fntvplus.log 跨版本追加的问题；client.log 本就启动清零）。go build/vet/test 全绿，fpk 重打 v0.54.0 |
| lc-066 | 2026-09-08 | v0.55.0 修删骨架后刷新「先原生页 1-1.5s 再切海报墙」裸奔闪现：bootCover 重写为极简进度条遮罩——#root visibility:hidden + 顶部 3px 流动进度条（观感=原生进度条加载，无骨架布局/shimmer），轮播注入(carouselInited)/STRM 提示(carouselLoadedButNone)/离开首页/2.5s 硬兜底任一命中即揭开。embyWall.ts 恢复 armBootCover() 调用。Playwright 冒烟全绿（ERRORS=none）。fpk 重打 v0.55.0 |
| lc-067 | 2026-09-08 | v0.56.0 每日放送数据本地持久化+刷新设置：①hotUpdates 新增 localStorage 持久化缓存（fntv:hot-cache-{bangumi,tmdb,douban}，存 {ts,items}），loadBg/loadTm 缓存优先——未超刷新间隔直接用缓存渲染（底部显示「数据更新于 …·本地缓存」），过期才重新拉取；②刷新间隔可配 1-7 天（localStorage fnos-hot-refresh-days，默认 1 天）；③设置面板外观卡新增「每日放送」组：显示开关（fnos-show-daily+fntv:daily-toggle 事件，网页端此前缺失）+ 刷新间隔 select + 立即刷新按钮（fntv:hot-refresh 事件 → hotUpdates 清三源缓存 + 强制重拉当前源，浮层没开也预拉）。Go 端无缓存不受影响（前端持久化）。Playwright 冒烟全绿。fpk 重打 v0.56.0 |
| lc-068 | 2026-09-08 | v0.58.0 修首页卡片圆角消失+整体下沉（用户截图：继续观看卡片底部直角+进度条突出）：根因=glassUI（云母玻璃增强，v0.30.0 全插件挂载时网页端生效）**默认强制开启**（lc-1081 schema v3 迁移强制回落 enabled=true），玻璃规则 `html[data-fntv-glass] [class*="card"]` 命中所有卡片——磨砂半透明底+border:none+大软阴影但**不带圆角** → 卡片直角/圆角视觉消失；②b 规则给海报行补 padding-bottom:24px + 背景层改布局 → 整体下沉。修：glassUI readSettings 的 enabled 加网页端门禁（window.__FNTV_WEB__ 强制 false，applyGlass 走摘属性分支原生还原）；桌面版不受影响。附带产出二分对比包 fntvplus-0.57.0-前端v043对比测试.fpk（前端=v0.43.0 payload+后端=v0.56.0，未用可删）。Playwright 冒烟全绿。fpk 重打 v0.58.0 |
| lc-069 | 2026-09-08 | v0.59.0 轮播样式4去上下黑阴影：.fntv-s4-bg::before 的自下而上黑渐变（底部 .95 黑→顶部 .02，用户截图上下均现暗色）大幅减轻收窄——只留底部轻衬底（.42→.16→52% 处透明），保证标题/简介/按钮白字可读，图片主体与顶部完全无暗色。样式 2 的同款渐变未动（用户当前用样式 4）。fpk 重打 v0.59.0 |
| lc-070 | 2026-09-08 | v0.60.0 每日放送设置独立成卡+开关胶囊化：①从外观卡拆出独立 section('每日放送')（挂在「外观」分类下），不再内嵌外观卡尾部；②显示开关从原生 checkbox 改为胶囊 track+knob 自绘（与「剧集详情页美化」同款——原生 checkbox 深色主题显示异常，v0.24 已有先例），加副标题说明；③间隔 select/立即刷新按钮/提示随卡迁移。Playwright 冒烟全绿。fpk 重打 v0.60.0 |
| lc-071 | 2026-09-08 | v0.61.0 设置分类精简（用户要求）：删除「播放」分类（lc-054 删手柄后仅剩跳过片头片尾一张卡）；跳过片头片尾卡并入「通用」（原「外观」更名）；分类 6→5（通用/弹幕/账号与网络/诊断与日志/关于）。Playwright 冒烟 PANE_COUNT=5、ERRORS=none。fpk 重打 v0.61.0 |
| lc-072 | 2026-09-08 | v0.62.0 TMDB 剧集信息持久化缓存（用户要求：一级/二级详情页不要每次重新拉取）：对齐桌面版 lc-926——tmdb:show（一级详情聚合）与 tmdb:season-episodes（二级季页双语分集）成功结果落盘到 config 同目录 tmdb-cache/（TRIM_PKGETC/tmdb-cache/，原子写 tmp+rename），缓存永久有效（桌面版 TTL 10 年等效，TMDB 内容静态），force=true（卡片「刷新」按钮）绕过重拉，失败不落盘。缓存键=md5(媒体类型+ID或标题/年份+季号)。config 包新增 Dir()（暴露 TRIM_PKGETC）。前端无需改动（tmdbCard 的 force/刷新按钮、fetchedAt/fromCache 消费本就齐备）。go build/vet/test 全绿，fpk 重打 v0.62.0 |
| lc-073 | 2026-09-08 | v0.63.0 修每日放送 TMDB 源海报大面积失败+「部分数据源获取失败」：TMDB 图片/发现请求单路失败无兜底——handleTMDBImage 仅 tmdbClient() 一路（lc-052 后=代理优先，代理对 image.tmdb.org 慢/失败时无退路），tmdbDiscover 两路也无重试。修：①tmdbClient 拆出 tmdbDirectClient()（免梯子直连专属，未开启/无 IP 返 nil）；②handleTMDBImage 多路尝试：自定义代理(12s) → 免梯子直连 IP → 系统直连，任一成功即返回，全失败才 502（带各路错误）；③tmdbDiscover movie/tv 两路各加重试一次（600ms 退避，桌面 getWithRetry 同语义）。go build/vet/test 全绿，fpk 重打 v0.63.0 |
| lc-074 | 2026-09-08 | v0.64.0 修每日放送 Bangumi 源无图：Bangumi 海报（lainpic.bgm.tv）走 tmdb:image 通道，但 handleTMDBImage 域名白名单只放行 image.tmdb.org → bgm 域名直接 400，前端失败后又无降级 → Bangumi 源全无图。修：①handleTMDBImage 放行为通用图片代理（image.tmdb.org/t/p/ + *.bgm.tv，请求统一带浏览器 UA）；②hydratePosters 代理失败降级直链（bgm.tv 国内可直连无防盗链 → el.src=直链；豆瓣已有 douban:image 通道不动；TMDB 直链被墙不降级保持留空）。go build/vet/test 全绿，fpk 重打 v0.64.0 |
| lc-075 | 2026-09-08 | v0.67.0 弹幕数据链接通（网页端弹幕核心）：桌面版弹幕渲染器 danmakuWeb（canvas 引擎+控制栏三按钮）已随插件挂载，缺的只是 danmaku:prepare 数据。新建 bridge/danmaku.go 移植 bili_danmaku.js 核心链（第一版经典稳态链）：WBI 签名（nav wbi_img + ENC 重排 mixin + md5，会话缓存）→ wbi/search/type PGC 搜索（番剧1/国创4，标题相似度打分+季数加权）→ pgc/view/web/season 选集（序号直取→标题含集数→首集）→ 影视区 media_ft+bvid view 兜底 → list.so?oid=cid 弹幕 XML 解析（time/type/color/text，正则+转义还原，按 time 排序）→ 屏蔽过滤（biliDanmakuBlockTypes 类型 + biliDanmakuBlacklist 黑名单）→ 磁盘缓存（danmaku-cache/，存未过滤原始条目）→ 返回桌面同形状（含 maxScreen 读 settings）。shim danmaku:prepare：前端直连 play/info 解析元数据（title/ep/season/isMovie，同 playSync 直连路径）→ 后端；candidates/pick 占位（网页端暂不支持手动选定）。已知限制：弹幕拉取走经典 list.so（桌面用 seg.so protobuf 分段，弹幕量略少）；danmu_api 自建优选源未接（后续）。**Go regexp 不支持 (?!...) 负向先行**——ep 标题正则改 FindStringSubmatchIndex+手动校验后继字符（init 期 panic 会炸整个测试包）。go build/vet/test 全绿，fpk 重打 v0.67.0 |
| lc-076 | 2026-09-08 | v0.68.0 修季页「剧集信息获取失败：缺少标题且无 TMDB id」：季条目的 getEditDetail 元数据经常不带 tmdb_id、标题还是「第 1 季」这类系统命名（_isSysTitle 过滤掉）、DOM 回退也拿不到剧名 → 请求全空报错。修：loadShowMeta 在季自身缺 tmdbId/标题时向上查父级（剧）条目（getEditDetail(parent_guid)）补全——父标题（真名）+ 剧的 TMDB 编号，与已有 seasonNumber 组合即可精确请求（TMDB 缓存键随之正确）。fpk 重打 v0.68.0 |
| lc-077 | 2026-09-08 | v0.69.0 修「所有播放按钮点击无反应」（其他功能正常、控制台无报错）：根因=playMaskButton（外部播放器 MPV/Pot 播放链）OnReady **无条件**安装全局播放点击拦截器（lc-1076：press+click 捕获阶段 stopImmediatePropagation 接管所有播放入口——首页卡片播放图标/详情选集/下一集/播放按钮 mask）→ 发 ipcRenderer.send('play-movie') 给 MPV → 网页端 shim 对 play-movie 是 no-op → 拦截"成功"但什么都不发生。修：installPlayClickInterceptor 加网页端门禁（window.__FNTV_WEB__ 直接 return 不装拦截器）→ 播放点击回归 fnOS 原生播放器；桌面版外部播放链不变。连带确认 skipInject 的 play/info 假响应拦截唯一触发方就是 playMaskButton（插件跳过后无人置 externalPlayActive）。Playwright 冒烟全绿。fpk 重打 v0.69.0 |
| lc-078 | 2026-09-09 | v0.70.0 跳过片头/片尾修好（网页播放生效）+ 外部播放器残留清理：①新建 bridge/skip.go 移植桌面三级外网链——AniSkip（MAL id 映射链：Bangumi v0 中文搜索取日文名→Jikan/AniList 双路；30 天内存缓存；episodeLength ±1/±2 阶梯重试；recap 透传）→ theintrodb v3 兜底（tmdb_id/imdb_id+season+episode+duration_ms）→ fnOS 语义换算（skipStart=OP 尾/intro 尾；skipEnd=总时长−ED 尾，outro≥半长判异常仅填片头）；fnOS skipinfo 读写由**前端直连**（双前缀 /v/api/v1 与 /api/v1 尝试）。shim skip:fetch-and-fill 接通（直连 play/info 元数据→fnOS 真值→后端外网链→写回）。②外部播放器残留清理：web-entry 摘 playButton/playChoice/playMaskButton 挂载（MPV/Pot 播放链，lc-077 已门禁）；embyWall 删「播放器」卡构建段 168 行（MPV/Pot 路径+ICC）+ 诊断导出播放器段 + 组件日志开关 mpv/potplayer/media 三项；Go 新路由 /bridge/skip/external。保留：skipInject（跳过数据填充，已生效）、autoplayNext、shim play-movie 等分支（nav/inject 外放按钮仍有调用方）。go build/vet/test 全绿，Playwright 冒烟 PANE_COUNT=5/ERRORS=none。fpk 重打 v0.70.0 |
| lc-079 | 2026-09-09 | v0.71.0 修网页端弹幕/跳过片头「进播放页不生效」：根因=网页端入口（web-entry boot）只派发 OnReady，**从不派发 OnDomChange**——桌面版由 preload/index.ts 的 MutationObserver（body subtree）派发；danmakuWeb（进播放页挂弹幕引擎/按钮）、skipInject（SPA 切播放页触发跳过填充）、danmakuHeat、a11y 等插件的播放页挂载全依赖 OnDomChange → 网页端这些功能在首次 OnReady 时机之后进播放页永不触发（弹幕数据链 v0.67 已通但渲染器从未挂载）。修：boot 补 MutationObserver 派发 OnDomChange（body subtree childList，插件各自带防抖）。此缺口也可能影响 autoplayNext 等其余 OnDomChange 插件（一并恢复）。Playwright 冒烟全绿。fpk 重打 v0.71.0 |
| lc-080 | 2026-09-09 | v0.73.0 修演员详情「fnOS 请求失败：dial tcp 127.0.0.1:5666 connection refused」：bridge 的 fnOS 调用固定用**启动推导上游**（127.0.0.1:5666），而用户在管理页配置的上游覆盖（127.0.0.1:18888）只被反代主链路每请求读取——两条链路不同源。修：bridge 新增 effectiveUpstream()（config.Upstream 覆盖优先，回退启动推导值，TrimRight 防双斜杠），三处消费点统一替换（handleFnOS 桥/bangumiSyncProgress play-info/callFnOSJSON——演员作品/跳过/同步/豆瓣全走后者）。与 lc-006（管理页热改上游）语义对齐。go build/vet/test 全绿，fpk 重打 v0.73.0 |
| lc-081 | 2026-09-09 | upstream 消费点全面审计+回归测试：四项审计全过——①bridge 内 b.upstream 残留仅 effectiveUpstream 回退（设计内）；②Upstream 静态保存仅 config.Get() 快照与 admin.Info（StatusAPI 显示回退，effective 实时）；③实时读取点齐全（proxy 每请求×3、bridge effectiveUpstream、admin 状态页）；④payload 不嵌上游（相对路径）。新增 effective_upstream_test.go 回归测试（未配置回退推导值/热改立即生效/尾斜杠归一/清空回退），锁死 lc-080 行为。纯 Go 侧测试，fpk 不变（v0.73.0） |
| lc-082 | 2026-09-09 | v0.74.0 修「TMDB combined_credits 失败：EOF」（演员作品加载失败）：/find 成功紧接着 combined_credits EOF——典型代理 keep-alive 复用已被服务端关闭的连接（第一请求成功、下一个 EOF）。根因=tmdbGet 单路（lc-052 后代理优先）网络错误无兜底。修：tmdbGet 重写为多路尝试（自定义代理 → 免梯子直连 IP → 系统直连）——**网络层错误（EOF/超时/重置/解析失败）自动换下一路**；服务器有响应（401/429 等）不换路原样返回（保留 Key 格式诊断）；全部失败返回带各路标识的最后错误。惠及全部 TMDB API 调用（find/credits/discover/show/img 元数据）。与 lc-073（图片多路）配对成完整 TMDB 多路兜底。go build/vet/test 全绿，fpk 重打 v0.74.0 |
| lc-083 | 2026-09-09 | v0.75.0 复刻 lc-1118（原项目未推的弹幕增强）：danmu_api 自建弹幕接口客户端完整移植（新文件 danmu_api.go）——三端点 search/anime（**只收主名归一化精确相等+季号一致**，tier 排序 bilibili 源优先；关键词整串→番名首段回退）、bangumi/{id}（分集）、comment/{id}?format=xml（与 list.so 同构共用 parseDanmakuXML）；prepare 前置 danmu 优选（自建源启用时先试，命中直接返回 source=自建源(danmu_api)；未命中降级 B站链路）；candidates 真实现（自建源候选 dmapi:<episodeId> 伪 id → 未命中降级 B站视频区 media_ft 前5条）；pick 真实现（dmapi: 前缀直取 / bvid→view 拿 cid→list.so，选定结果落 danmaku-cache 同键缓存记住选择）。原项目拉取核实：origin/release 无新提交（本地领先 34 个），此前移植基于即最新代码。go build/vet/test 全绿，fpk 重打 v0.75.0 |
| lc-085 | 2026-09-09 | v0.76.0 同步桌面弹幕渲染端最新（lc-1105~1119 渲染侧全部增量）：danmakuWeb.ts（1198→1982 行：lc-1105 弹幕重做[数据层排序/缓存源校验/屏蔽过滤+渲染核心修复]、lc-1106 日志刷屏+按钮反色、lc-1107/1110/1112/1113 弹窗改飞牛原生折叠形态、lc-1111 收起条件收窄、lc-1114 开关样式、lc-1116 起播白圈全藏、lc-1118 手动搜索段）+ danmakuHeat.ts + i18n.ts（弹幕面板/手动搜索等新词条；代理描述文案重放去掉 SOCKS5）。fntvplus 特化文件（bootCover/images/styles/tmdbCard/hotUpdates/playMaskButton/glassUI/embyWall）保留本地版不覆盖。验证假象记录：esbuild 中文转义+新版改名 id 导致 grep 旧特征=0 的双重假象，用新版特征串（fntv-dm-panel-style/fntv-dm-list）确认新版已进包。fpk v0.76.0 |
| lc-087 | 2026-09-09 | v0.77.0 修样式4海报外圈黑线（用户截图：轮播左右/上下边缘暗线，有些有有些没）：lc-069 减轻黑渐变后，卡片自身的深色占位底(#1e1b17)+1px 白 7% 边框+大扩散黑阴影（0 34px 80px rgba(0,0,0,.5)/0 30px 60px .42）暴露出来——浅色海报与浅色页面背景下呈"外圈黑线"，随相邻卡片位置与海报深浅时隐时现。修：卡片去 1px 边框（border:none）+ 底影减淡（0 18px 44px .22）+ active 阴影减淡（0 22px 56px .28）。fpk 重打 v0.77.0 |
| lc-088 | 2026-09-09 | v0.78.0 修自建弹幕接口保存：①shim 补 settings:set-danmu-api 特例（桌面 handler 收 {enabled,base} 对象存两键 danmuApiEnabled+danmuApiBase——此前无特例，通用逻辑把整个 payload 存进 danmuApi 单键、地址键从未写入=无法保存；含桌面同款地址格式校验回 ok:false）；②Go danmuBase 读键改 danmuApiBase（原 "danmuApi" 是被污染键）；③提示文案「下次 MPV 播放时生效」→「下次播放时生效」（网页端无 MPV）；④删「打开弹幕文件夹」按钮（MPV 弹幕落盘目录，网页端不存在）。回填链走 settings:get 全量（GetMap 含两键 ✓ 无需特例）。go build/vet 全绿，Playwright ERRORS=none，fpk 重打 v0.78.0 |
| lc-089 | 2026-09-09 | v0.79.0 修「自建弹幕接口还是无法保存」：lc-088 的 set-danmu-api 特例**插错了位置**——加在 test-danmu-api（L415 区域）旁，而 settings:set-danmu-api 通道进来时先被 L204 的 startsWith('settings:set-') 大分支截胡 return（通用逻辑把 payload 存进 danmuApi 单键）→ 特例永远不可达。修：特例块搬进 set- 大分支内（set-dandanplay-credentials 之前、通用 return 之前）。执行顺序已验证（payload 中特例@21015 < 通用@21763）。**教训：shim 的通道特例必须放进对应 startsWith 大分支内部、通用 return 之前——放外面=死代码**（lc-053 的 set-custom-proxy 当时插对了位置，本轮没复核插入点）。fpk 重打 v0.79.0 |
| lc-090 | 2026-09-09 | v0.80.0 复刻 lc-1115/1116 弹幕测试分层诊断（用户在原项目加的日志排查功能）：①新建 bridge/danmu_diag.go——danmuDiag 分层归因（地址形态[回环/私网/CGNAT/公网/域名分类]→DNS→TCP→TLS[证书错误人话归因表]→服务应答[httptrace 分段 dns/tcp/tls/首字节 + repeats 次抖动]），base 一律脱敏（TOKEN 在路径段），错误码→人话归因字典；②shim settings:diag-danmu-api → /bridge/danmu/diag；③设置面板自建源卡加「运行分层诊断」按钮（结果 pre 就地逐行展示：✓/⚠/✗ label+耗时+明细+hint）。原项目对照：handleDiagDanmuApi + danmuApi.diagnose（DNS/TCP/TLS probe + classifyHost + 路由覆盖——后两简化）。**坑**：python heredoc 写 `join('\n')` 被展开成真换行 → esbuild Unterminated string——**含转义序列的代码一律用 Edit 工具写，不用 bash heredoc python**。go build/vet/test 全绿，Playwright ERRORS=none。fpk 重打 v0.80.0 |
| lc-091 | 2026-09-09 | v0.81.0 自建源尝试结论可观测（用户报弹幕走 B站 未命中、自建源疑似未参与）：danmuAutoFetch 返回带原因（未启用/搜索无精确匹配条目/候选均无弹幕/命中），prepare 的 B站 失败错误信息前置自建源结论（「自建源(未启用)；未找到匹配的B站弹幕」等）→ 弹窗「备注」直接可见自建源到底试没试、卡在哪。排障路径明确：①装 v0.79+（此前版本保存修复无效）；②自建优选源开关开+地址保存成功；③播放后备注会给出自建源结论。go build/vet/test 全绿，fpk 重打 v0.81.0 |
| lc-092 | 2026-09-09 | v0.82.0 外部播放器残留终清（用户日志实锤两 bug）：①SETTINGS refresh [players] 段 ReferenceError: mpvPath is not defined（每次开设置面板必抛——lc-078 删播放器卡漏清回填引用）→ 删 seg('players') 回填段；②nav/inject.ts 播放页控制栏「🎬 MPV」按钮注入（MutationObserver 常驻+注入日志）→ 删（网页端播放统一 fnOS 原生）；③删除 playButton.ts/playChoice.ts/playMaskButton.ts 三个外部播放插件源文件（v0.70 已摘挂载的死代码；skipInject 保留——跳过片头填充职责独立）。dist 验证：mpvPath 1 处（i18n 词条 key 无害）/播放页 MPV=0/play-movie send=0。fpk 重打 v0.82.0 |
| lc-093 | 2026-09-09 | v0.83.0 修覆盖更新后侧边栏「观影记录」消失过一会才出现：按钮注入成功后 watchHistory 的 MutationObserver 即 disconnect，之后 fnOS（React）重渲染侧栏冲掉容器/按钮时无人重挂，只剩 3s keepAlive 轮询兜底（消失到重现最长 3s+，覆盖更新后首次加载时序变化最明显）。修：加常驻 MutationObserver（被删立即重挂；injectEntry 幂等无循环风险），3s keepAlive 保留兜底。fpk 重打 v0.83.0 |
| lc-094 | 2026-09-09 | v0.84.0 修自建源「搜索无精确匹配条目」（同配置原项目正常）：**success 布尔判断写反**——桌面 TS 是 `j.success === false` 才算失败（success=true/无字段都算成功），Go 移植写成 `succ` 为 true 就 break（当失败）→ danmu_api 正常响应 {"success":true,"animes":[...]} 被当成服务异常 → 搜索永远空 → "自建源(自建源搜索无精确匹配条目)"。修：`if succ, ok := j["success"].(bool); ok && !succ { break }`。go build/vet/test 全绿，fpk 重打 v0.84.0 |
| lc-095 | 2026-09-09 | v0.85.0 修动漫弹幕搜不到+手动搜索不自动填标题：①根因=B站 番剧搜索**必须登录态 Cookie**（桌面 _wbi_pgc_search 首行：无 COOKIE 直接跳过，匿名 media_bangumi 必返回 0）——用户未配 biliCookie → 动漫必搜不到。修：biliSearchPGC 加无 cookie 直接返回（省无效请求）+ 失败信息明确提示「未登录B站——番剧/动漫需登录态搜索，请在设置→B站弹幕登录扫码后重试」；②手动搜索不自动填标题=prepare 失败分支未返回 meta（渲染端自动填标题取 meta.searchTitle）→ 失败也带 meta（searchTitle/ep/season/isMovie/count=0）。go build/vet/test 全绿，fpk 重打 v0.85.0 |
| lc-096 | 2026-09-09 | v0.86.0 清 B站弹幕登录卡 MPV 开关与文案：①删「启用 MPV B站弹幕搜索」开关（联动 MPV uosc conf，外部播放器残留；网页端 B站搜索开关在播放页弹幕设置里）+ 回填段 swMpvBiliSearch 引用（防 ReferenceError）；②MPV 文案清：弹幕屏蔽卡提示/弹弹play 三处提示/自建源提示「下次 MPV 播放时生效」→「下次播放时生效」/关于描述去「支持 MPV 播放器」。dist 验证 swMpvBiliSearch=0/set-mpv-bili-search=0。fpk 重打 v0.86.0 |
| lc-097 | 2026-09-09 | v0.88.0 补 B站 视频区搜索（用户双图对比实锤：MPV 命中「视频区(UP主搬运)」BV18bbe6pEr3 122条，网页版同番搜不到）：danmaku.go 新增 biliSearchVideos（WBI search/type search_type=video，剥 em 高亮标签）——prepare 兜底改「视频区优先 → media_ft」、candidates B站 兜底同步视频区优先；meta 全分支带 cookieStatus（详情「登录状态」不再显示 —）。此前只搜番剧区+影视区，漏了 UP 主搬运这一弹幕主力。go build/vet/test 全绿（注意 go test 须在 src/go 目录跑，根目录无 go.mod），fpk 重打 v0.88.0 |
| lc-098 | 2026-09-09 | v0.89.0 修「登录了B站还提示未登录」：**cookie 键名不一致**——扫码登录（bili.go）存 `bili_cookie`（下划线，桌面 config 同名），lc-095 弹幕链写的读 `biliCookie`（驼峰）→ 读不到 → 误报未登录+番剧搜索被跳过。修：danmaku.go 全部 5 处读键统一为 bili_cookie（danmakuGetJSON 请求头/biliSearchPGC 跳过/cookieStatusOf/danmuFetchItems/hasCookie 提示）。go build/vet/test 全绿，fpk 重打 v0.89.0 |
| lc-099 | 2026-09-09 | v0.90.0 修轮播「开始播放/详情」永远跳一级详情页：resolveSeasonHref 依赖 media:season-guid 通道查季 guid，shim 里该通道是 no-op → 永远回退一级。修：shim 直连实现（fnOS item/list，桌面 libraryIndex 同参数 parent_guid/exclude_folder/sort_title，type==='season' 优先、非 episode/movie 兜底），带 genAuthx+credentials；结果经 href.ts 缓存（show.id→href）。skip:next-episode no-op 分支保留。fpk 重打 v0.90.0 |
| lc-100 | 2026-09-09 | v0.91.0 补回「轮播图标题替换为 Logo」开关（用户要求挂通用卡）：logo.ts 渲染链（fetchTMDBLogo/上传/applyCarouselLogoNow）一直都在且默认启用（S.carouselLogoEnabled=true），lc-041 精简把开关和回填一起删了 → 用户找不到入口。修：外观卡（通用分类）轮播样式切换之后插入开关（桌面同款文案+持久化+applyCarouselLogoNow 即时生效）+ SETTINGS refresh 补回填（S.carouselLogoEnabled = s.carouselLogoEnabled !== false）+ shim SETTINGS_KEY_MAP 加 'carousel-logo'→'carouselLogoEnabled' 映射（否则驼峰化成 carouselLogo 与 state 键不符）。验证：_swLogo=5/set-carousel-logo=2 进包。fpk 重打 v0.91.0 |
| lc-101 | 2026-09-09 | v0.92.0 移植「首页 Logo 自定义」（原项目 customLogo 插件 lc-1046）：①拷贝 24 张平台预设 PNG → src/go/internal/bridge/logos/ + Go embed（logos.go，文件名白名单防穿越，7 天缓存）经 /bridge/logos/<file> 提供；②customLogo.ts 重写为网页版（原桌面版依赖 fs/path/__dirname/#tb-logo 在浏览器顶层执行即崩=从未工作过）——预设 chips（国内/国际分组，点选即存即用）+自定义上传（localStorage dataURL ≤1.5MB）+恢复默认；③目标=fnOS 原生首页 logo（试探单 img[src*=logo]→header img→table img，未中则 console 提示回传 outerHTML）；④UI 挂设置面板通用卡（轮播 Logo 开关之后）。坑：embyWall.ts 的 import 相对路径（./customLogo 非 ./plugins/customLogo）；shim SETTINGS_KEY_MAP 曾重复键（esbuild duplicate-key 警告）。fpk 重打 v0.92.0 |
| lc-102 | 2026-09-09 | v0.93.0 修首页 Logo 自定义定位（用户反馈"功能有问题"——上一版宽泛试探单 img[src*=logo]/header img/table img 误中头像等无关图片）：findHomeLogo 重写——限定首页顶部区域（top<160px、宽 8~600）+ 「飞牛」特征（alt/title/src/class 含 飞牛/fnos/logo/brand，正则）→ 弱匹配兜底（顶部孤立小 img 非链接内）→ 文字版兜底（TreeWalker 找含「飞牛影视」文本的最深层元素，文字 logo 换 innerHTML 为 img）。恢复默认还原首次保存的原始 src/innerHTML（ORIG_ATTR）。文件名 logo 定位也支持文字版。fpk 重打 v0.93.0 |
| lc-103 | 2026-09-09 | v0.94.0 首页 Logo 改「注入+替换」双模（用户澄清：fnOS 原生首页没有 logo，桌面端的 logo 是 titlebar 自注入悬浮元素——已核对桌面 titlebar.ts #tb-logo：body 顶层 fixed top:72px 居中 height:30px 仅首页显隐 4s 守护）：applyLogo 双模——A.原生顶部有 logo（lc-102 特征定位）→ 原位替换可还原；B.原生没有 → 注入悬浮 logo（INJECTED_ID=fntv-home-logo，桌面同款样式，OnDomChange 刷新显隐，离开首页 hidden）。默认态 resolveLogoSrc 返回 /bridge/logos/fntv_default.png（iconfntv.png 已 embed）。fpk 重打 v0.94.0 |
| lc-104 | 2026-09-09 | v0.95.0 按用户要求完整搬运桌面版 customLogo（此前 v0.92-0.94 自写简化版用户不满意"设置乱/没 logo"）：整文件复刻桌面 517 行（设置卡片[当前缩略图+当前项标签+选择预设/上传自定义/恢复默认]+预设选择弹窗[实时预览/分组网格/点选即生效自动关闭]+亮度自适应垫底[canvas 检测白色主体 logo 垫深色底]），仅五处网页适配：①imports 去 fs/path+LOGO_API_BASE；②删 PRESET_DIR；③presetDataUri fs→URL（/bridge/logos/）；④default→fntv_default.png(embed)+registerDefaultLogo 空实现；⑤锚点注入(#fnos-appearance-ctrl 桌面锚)改导出 buildCard/refreshCard 由通用卡挂载+applyLogoToDom 自建 #tb-logo 宿主（网页无 titlebar，桌面同款样式/4s 守护/首页显隐）。embyWall 通用卡挂 buildCard + 回填段 refreshCard 同步。fpk 重打 v0.95.0 |
| lc-105 | 2026-09-09 | v0.96.0 修首页 Logo 位置偏下（用户截图：logo 在白色顶栏下缘）：桌面 top:72px 是桌面布局（body padding 32 + navbar 40）——fnOS 网页顶栏实测高 80px（lc-875 日志 h-[80px]）→ 网页端注入位置改 top:40px（顶栏垂直中心）。fpk 重打 v0.96.0 |
| lc-106 | 2026-09-09 | v0.97.0 侧边栏展开动画补全（用户要求加动画）：考古发现 v329 的 .drawer-open 类驱动机制**一直缺 CSS 规则**（类加了样式没变=瞬切无动画）。补注入 fntv-drawer-anim-style：背板 opacity 淡入 + 面板 translateX(-28px)→0 滑入淡入（340ms cubic-bezier(.22,1,.36,1) easeOut），收起反向（与既有 340ms display 移除定时匹配）。**工具坑三犯**：python heredoc 写 join('\n') 的 \n 转义再次被展开成真换行 → esbuild Unterminated string；最终用 chr(92)+'n' 规避——**含 \n 转义的代码写入必须 Edit 工具或 chr(92) 拼接**。fpk 重打 v0.97.0 |
| lc-107 | 2026-09-09 | v0.98.0 修关闭侧边栏时毛玻璃瞬消突兀：面板毛玻璃是 applySidebarGlass 注入的 inline !important backdrop-filter——不参与 .drawer-open 类过渡 → 面板渐隐滑出时模糊原样挂到最后一帧才瞬消。修：①applySidebarGlass 面板常驻 inline transition 加 backdrop-filter .3s ease（与 transform/opacity 并列）；②animateCloseDrawer 关闭时把面板 backdrop-filter 过渡到 blur(0px)（随滑出同步渐隐）。fpk 重打 v0.98.0 |
| lc-108 | 2026-09-09 | v0.99.0 修「Logo 更新后要再设置里重新设置才显示」：v0.95 完整搬运时删掉了 OnReady 锚点注入 handle，而网页端 logo 宿主（#tb-logo）由本模块自建、**只在 applyLogoToDom 被调时创建**——桌面版靠 titlebar 挂宿主无此问题，网页端删了启动调用后刷新/重开首页 Logo 消失、进设置点一次（触发 applyLogoToDom）才出现。修：补 OnReady/OnDomChange → applyLogoToDom（幂等：宿主在只换 src/显隐）。fpk 重打 v0.99.0 |
| lc-110 | 2026-09-09 | v1.0.0 诊断卡全面对齐 web 端（用户报「与现在 web 端不符合」）：旧诊断渲染桌面字段（isPackaged/App 路径/mpv-user.conf/uosc_danmaku.conf/登录背景路径/更新打烊时间戳——网页端全无意义）。重写 loadDiag：数据源=/api/status（版本/Payload hash/上游+连通性/增强注入/服务器时间）+ settings:get（豆瓣/Bangumi/B站登录态/自建源脱敏/屏蔽词/滚轮/轮播）+ location/navigator（访问入口/浏览器 UA）；自建源地址脱敏（TOKEN 在路径段）。桌面诊断通道 settings:diagnostics 不再调用。里程碑：v1.0.0。fpk 重打 v1.0.0 |
| lc-111 | 2026-09-10 | v1.0.1 按用户要求去 fpk 桌面应用 + 管理页直连入口：①manifest 删 desktop_uidir/desktop_applaunchname 两行（不再注册 fnOS 桌面"设置应用"图标；fnpack 会自动补空值键——闭源官方打包器无法改，空值是否出图标待实测，若仍出下一轮试 micro_app=false）；②proxy.go 加 /admin 与 /admin/ 路由（管理页直连：http://<NAS>:22350/admin，Go 后端 0.0.0.0 监听本来就可局域网直连）；/app/fntvplus/admin 经 fnOS 网关的旧入口保留兼容（admin UI 内部 API 路径带前缀，两种入口都工作）。fpk 重打 v1.0.1 |
| lc-112 | 2026-09-10 | v1.0.2 图标=影视入口、删设置图标（用户要求"点击图标直接跳转影视页面而不是设置页面"）：真相比预想简单——app/ui/config 定义了两个桌面入口：fntvplus.Application（url=/v/ 影视页，一直存在）与 fntvplus.Settings（/app/fntvplus/admin/ 设置页，即"多的那个"）。修：config 删 fntvplus.Settings 条目，manifest desktop_applaunchname 指回 fntvplus.Application。图标点击 = 直接进影视页；管理页走 /admin 直连。验证：fpk 内层 ui/config 仅含 Application 条目。fpk 重打 v1.0.2 |
| lc-113 | 2026-09-10 | v1.0.3 卸载选项窗口（用户要求对齐其他应用：卸载时弹「保留数据/删除数据」）：照 qmediasync 范式实现 fnOS 卸载向导——①wizard/uninstall（JSON：radio 字段 wizard_delete_data，默认「保留数据」，选项 保留数据/删除数据）；②cmd/uninstall_callback 读环境变量 wizard_delete_data=yes 时清空 $TRIM_PKGVAR（@appdata/fntvplus，设置/弹幕缓存/TMDB 缓存全在此），带路径后缀防误删防护；保留数据=不删（@appdata 按应用名保留，重装原样恢复）。已核对 fnOS 向导 schema（qmediasync 同构：stepTitle/items[type=radio,field,label,initValue,options,rules]），wizard_delete_data 经环境变量传入脚本。fpk 重打 v1.0.3 |
| lc-114 | 2026-09-10 | v1.0.4 卸载向导文案优化（用户要求加必要文字说明+优化显示）：label 扩为数据清单说明（界面设置/首页 Logo 选择/B站登录态/弹幕缓存[自建源+B站]/TMDB 缓存 + 卸载后果提示「影视页面将不再有界面增强」）；两个选项改带后果的完整措辞——「保留数据——重装时自动恢复全部设置与缓存」/「删除数据——彻底清空以上全部内容，不可恢复」。注：fnOS 向导样式由系统渲染（schema 无 style 字段），可优化面=文案结构；wizard/uninstall 位于 fpk 外层（非 app.tgz）。fpk 重打 v1.0.4 |
| lc-115 | 2026-09-10 | v1.0.5 卸载数据处理可核对化 + 依赖答疑：①uninstall_callback 加 fnOS 日志输出（选择结果/数据目录/删除动作——应用中心卸载详情可查，防"到底删没删"黑盒）；②依赖确认：包内=静态编译 Go 二进制（14.2MB 无动态链接器）+UI 图标+config，manifest/config/privilege/resource 零 node/python 依赖声明——运行时不需要 Node.js/Python（Node 仅开发构建工具），安装界面不显示依赖属正常。fpk 重打 v1.0.5 |
